<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

		<!-- Create Menu From view -->

        <record id="view_manual_sms_send" model="ir.ui.view">
            <field name="name">Send manual SMS</field>
            <field name="model">partner.sms.send</field>
            <field name="arch" type="xml">
              <form string="Manual SMS - Gateway: clickatell">
                    <group string="Gateway">
                        <field name="gateway" on_change="onchange_gateway(gateway,context)"/>
                    </group>
                    <group string="Condition SMS send" colspan="4">
                     	<group>
		                    <field name="month_birthday"/>
		                    <field name="type_card"/>
	   				    </group>
	   				    <group>
							<field name="state_ids" widget="many2many_tags" options="{'no_create_edit': True}"/>
						    <field name="password" invisible="1"/>
						</group>
					</group>
					<notebook>
	                    <page string="Message">
					    	<field name="text" colspan="4" nolabel="1"/>
	                   	</page>
	                   	<page string="Additional option">
   		                	<group>	  	 	
	           		          	<group>
	   		          	        	<field name="validity"/>
	   		          	        	<field name="classes"/>
	   		          	        	<field name="deferred"/>
	   		          	        	<field name="nostop"/>
	           		          	</group>
	   		          	        <group>
		                   			<field name="priority"/>
		                   			<field name="coding"/>
		                   			<field name="tag"/>
								</group>
							</group>
						</page>
					</notebook>
                    <footer>
                        <button name="manual_send" string="Send SMS" type="object" class="oe_highlight"/>
                        or
                        <button special="cancel" string="Cancel" class="oe_link"/>
                    </footer>
             	</form>
            </field>
        </record>

       <act_window name="Send manual SMS"
            res_model="partner.sms.send"
            src_model="res.partner"
            view_mode="form"
            target="new"
            key2="client_action_multi"
            groups="group_sms_user"
            id="action_manual_sms_send"
	   		view_id="view_manual_sms_send" />
    </data>
</openerp>
