<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

		<!-- Create Menu From view -->

        <record id="view_partner_sms_send" model="ir.ui.view">
            <field name="name">Send SMS</field>
            <field name="model">partner.sms.send</field>
            <field name="arch" type="xml">
              <form string="SMS - Gateway: clickatell" version="7.0">
                    <group string="Gateway">
                        <field name="gateway" on_change="onchange_gateway(gateway,context)"/>
                    </group>
                    <group string="Bulk SMS send" colspan="4">
                     	<group>
		                    <field name="mobile_to"/>
		                    <field name="app_id" invisible="1"/>
	   				    </group>
	   				    <group>
							<field name="user" invisible="1"/>
						    <field name="password" invisible="1"/>
						</group>
					</group>
					<notebook>
	                    <page string="Message">
					    	<field name="text" colspan="4" nolabel="1"/>
	                   	</page>
	                   	<page string="Additional option">
   		                	<group>	  	 	
	           		          	<group>
	   		          	        	<field name="validity"/>
	   		          	        	<field name="classes"/>
	   		          	        	<field name="deferred"/>
	   		          	        	<field name="nostop"/>
	           		          	</group>
	   		          	        <group>
		                   			<field name="priority"/>
		                   			<field name="coding"/>
		                   			<field name="tag"/>
								</group>
							</group>
						</page>
					</notebook>
                    <footer>
                        <button name="sms_send" string="Send SMS" type="object" class="oe_highlight"/>
                        or
                        <button special="cancel" string="Cancel" class="oe_link"/>
                    </footer>
             	</form>
            </field>
        </record>

       <act_window name="Send an SMS"
            res_model="partner.sms.send"
            src_model="res.partner"
            view_mode="form"
            target="new"
            key2="client_action_multi"
            groups="group_sms_user"
            id="action_partner_sms_send"
	   		view_id="view_partner_sms_send" />
            
       <!-- Inherit Partner Form -->
       
       <record id="act_to_partner_2_history" model="ir.actions.act_window">
			<field name="name">SMS History</field>
			<field name="res_model">sms.smsclient.history</field>
			<field name="view_type">form</field>
			<field name="view_mode">tree,form</field>			
		</record>
       
       <record model="ir.ui.view" id="partner_view_sms_button">
            <field name="name">partner.view.buttons</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form" />
            <field name="priority" eval="20"/>
            <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
            <field name="arch" type="xml">
            	<xpath expr="//div[@name='button_box']" position="inside">
                    <button type="action" 
                        class="oe_stat_button"
                        icon="fa-list"
                        name="%(act_to_partner_2_history)d" 
                        context="{'search_default_partner_id': active_id,'default_partner_id': active_id}">
                        <field string="SMS History" name="to_sms_history_count" widget="statinfo"/>
                    </button>
                </xpath>
            </field>
       </record>
       
    </data>
</openerp>
